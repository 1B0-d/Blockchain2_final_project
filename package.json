{
  "name": "gamefi-economy-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Blockchain Technologies 2 final project: GameFi economy protocol.",
  "scripts": {
    "solc:check": "solc --base-path . --include-path node_modules --abi --bin contracts/GameToken.sol contracts/GameItems.sol contracts/GameVault.sol -o build/solc --overwrite",
    "fmt:check": "echo \"forge fmt --check will run after Foundry is installed\"",
    "test": "echo \"forge test will run after Foundry is installed\""
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "solhint": "^5.0.5"
  }
}

